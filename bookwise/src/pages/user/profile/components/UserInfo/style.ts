import { styled } from "../../../../../../stiches.config";

export const UserInfoContainer = styled('div', {
    display: 'flex',
    width: '100%',
    height: '100%',
    flexDirection: 'column',
    marginLeft: '4rem',
    backgroundColor: 'red'
})