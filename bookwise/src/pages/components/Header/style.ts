import { styled } from "../../../../stiches.config";

export const HeadingContainer = styled("div", {
  paddingTop: "$10",

  h2: {
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "center",

  },
});
